/var/www/apps/<%= node['app_name'] %>/shared/log/*.log{
  missingok
  compress
  dateext
  delaycompress

  lastaction
    pid=<%= node[:app_deploy_dir] %>/shared/pids/unicorn.pid
    test -s $pid && kill -USR1 "$(cat $pid)"
  endscript
}
